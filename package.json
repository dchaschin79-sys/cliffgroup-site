{
  "name": "cliffgroup-site",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-ready static marketing site for Cliff Ops.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check server.js && node --check app.js",
    "start": "node server.js"
  }
}
